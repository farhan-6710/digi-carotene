export function AboutPage() {
  return (
    <section className="space-y-3">
      <h1 className="text-2xl font-semibold tracking-tight">About</h1>
      <p className="text-muted-foreground">
        Coffee Mustache is a lightweight admin interface prototype for cafe
        operations. Routing is powered by React Router v7 (imports from
        "react-router").
      </p>
    </section>
  );
}
